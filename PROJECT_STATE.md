# Project checkpoint — 2026-09-07

- **CURRENT VERSION:** recovered source 0.6.2; ZIP filename misleadingly says 0.6.0.
- **CURRENT BRANCH:** codex/normalize-repository; original default branch main at 078aa55.
- **CURRENT PRODUCTION:** https://aion-agent-core-live.onrender.com reports 0.7.1; /health OK, /readiness database=true and a2a_runtime=true. Newer source is not in the cloned main history.
- **Repository structure:** app/ FastAPI backend and services; alembic/ four migrations; tests/ REST/MCP/A2A and real startup; scripts/ checks and operational tools; root Dockerfile, render.yaml, requirements.in and hash-locked requirements.txt; .github/workflows/; docs/ audit. ZIP retained unchanged only for legacy compatibility/rollback.
- **Completed work:** full inventory and archive audit; backup git tag backup/pre-normalization-20260907; all 66 archive files extracted and byte-compared before edits; backend and migrations preserved; direct-source setup, lock, ignore rules, empty-value env inventory, agent directives, CI checks and startup regression prepared.
- **Current phase:** repository normalization and deployment reconciliation; no new product features.
- **Current critical task:** recover the actual production 0.7.1 source and deployment configuration, then reconcile before merging or changing production. Do not deploy recovered 0.6.2 over 0.7.1.
- **Known blockers:** GitHub publication requires user authentication/permission: git push has no local credentials, and connector create_branch returns HTTP 403 Resource not accessible by integration despite repository metadata reporting push rights. No remote branch, backup or PR was created. Render MCP additionally requires confirmation of My Workspace (tea-daehtv2d0e5s738ir540), already requested. Current service commands/deployed SHA and environment names remain unverified.
- **Known critical issues:** repository/production version drift; archived identity only deduplicates exact external_id and does not prove independent ownership; join check/insert is not atomic and capability insertion uses a second transaction; reputation is requester-reported; A2A task store and rate limits are process-local; archived M2-M4 count rows, not verified independent agents; payment records are intents only. Newer production has identity-resolution endpoints absent here; do not overwrite them.
- **Latest successful tests:** Python 3.12: 42 passed, no skips, also repeated in a second clean virtualenv installed with --require-hashes; includes fresh/repeated Alembic upgrade, schema comparison and actual Uvicorn health/stats with A2A required. pip check clean; readiness 24/24; heuristic secrets scan including historical ZIP blobs: zero findings. git diff --check clean after trimming one inherited migration docstring whitespace. See docs/REPOSITORY_AUDIT.md for validation limits.
- **Failed tests:** untouched archive: 40 passed, 1 failed due to obsolete result.parts expectation in A2A test; corrected to pinned SDK's result.message.parts. Initial readiness string check needed adapting to generated lock. Local failures resolved. Live 0.6.2 gate fails at version check because production is 0.7.1; this is unresolved version drift, not a production outage.
- **Deployment state:** production unchanged; no service/database mutation. Root-source Render configuration prepared, not applied. Docker unavailable locally; Linux CI and real production PostgreSQL compatibility must be assessed separately.
- **Architecture decisions:** preserve FastAPI/SQLAlchemy/Alembic and existing service modules; A2A primary agent-to-agent, MCP separate tool integration; GitHub direct sources authoritative; keep historical ZIP until verified cutover. No fabricated redesign or growth work.
- **Known product metrics:** live snapshot: M1=302 requests; raw identity rows=2, activated=2, returning=1; estimated unique external joined=1, activated=1, returning=1; duplicate rows=1. These are server-reported estimates, not independently verified retained-agent evidence. This audit performed public probes, no agent registration or outreach. Verified milestone of 10 retained independent agents is not established.
- **Next actions:** enable GitHub write authentication and push this branch plus backup tag; read remote key files and inspect Linux CI. Confirm Render workspace; inspect service provenance without secret values; recover 0.7.1 source; reconcile and test newer schema/core behavior; only then merge/cut over to direct-source deployment. Continue identity/deduplication and A2A conformance from reconciled code.

Critical path:

audit -> normalize GitHub -> reproducible deployment -> identity/deduplication -> A2A conformance -> core product loop -> matching -> external-agent validation -> metrics/retention -> security/reliability/testing -> machine payments -> 10 retained external agents -> growth
